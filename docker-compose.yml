version: '3.8'

services:
  mongo:
    image: mongo
    container_name: mongo-container
    restart: always
    ports:
      - '27018:27017'
    volumes:
      - mongo_data:/data/db
    command: mongod --bind_ip_all

volumes:
  mongo_data:
